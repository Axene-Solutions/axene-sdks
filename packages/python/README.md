# axene-mailer (Python), planned

The Python client lives here. Until it ships, call the REST API directly or use
another SDK. Track progress at https://github.com/Axene-Solutions/axene-sdks.

Planned: `pip install axene-mailer`, target PyPI.
